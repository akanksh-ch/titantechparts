services:
  frontend:
    image: node:slim
    working_dir: /app
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    command: >
      sh -c "npm install && npm run dev -- --host"
